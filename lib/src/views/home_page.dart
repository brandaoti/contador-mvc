import 'package:desafio003/src/views/counter/counter_view.dart';
import 'package:flutter/material.dart';

class HomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: CounterView(),
    );
  }
}
